判斷敵兵：從上一個狀態的位置，距離<=可移動距離 的範圍找, 並且因爲兵只會往前，所以不用考慮往後搜
先搜尋所有已知存在的兵 ， 確認位置後 ， 在 rawmap 上把它標為 (id+'a') ，即可與新加入的兵區別
new idea : 把所有點都變成二維坐標形式，就可以用計算幾何找

如果把敵人狀態假設為跟我完全一樣(cards, mana ...) 做模擬，成效或許不差，因為兩方都是不差的解

if I use MyState and OpsState , I should change MyCastle, OpsCastle in GameState to CastleList
